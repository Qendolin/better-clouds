# suppress inspection "UnusedProperty"
org.gradle.jvmargs=-Xmx8G
# suppress inspection "UnusedProperty"
org.gradle.parallel=true

loom.platform=[VERSIONED]
# Required bc. arch loom 1.8 isn't out yet
loom.ignoreDependencyLoomVersionValidation=true

# Fabric Properties
loom.yarn=[VERSIONED]
# this is optional
loom.yarn_patch_neoforge=
loader.fabric=0.16.10
loader.neoforge=[VERSIONED]
loader.forge=[VERSIONED]

maven_group = com.qendolin
archives_base_name = better-clouds

# Mod Properties
mod.version=[VERSIONED]
mod.release=release

# Common Dependencies
deps.yacl=[VERSIONED]
deps.minecraft=[VERSIONED]
deps.enhanced_celestials=[VERSIONED]

# Fabric Dependencies
deps.fabric_api=[VERSIONED]
deps.modmenu=[VERSIONED]

# NeoForge Dependencies

# Forge Dependencies
deps.mixin_extras=0.4.1

# Compat
deps.sodium_extra=[VERSIONED]
deps.iris=[VERSIONED]
deps.serene_seasons=[VERSIONED]
deps.fabric_seasons=[VERSIONED]